.lstrip() é um método de string que remove caracteres do lado esquerdo (left strip) da string

f-string - É uma forma moderna e mais legível de inserir valores de variáveis diretamente dentro de uma string, usando o prefixo f antes das aspas:
 print(f"\U0001F389 O número digitado foi: {number} \U0001F389")

 except ValueError - é usado para tratar erros quando o valor fornecido é inválido para uma operação, como converter texto em número.